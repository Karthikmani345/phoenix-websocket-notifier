// import { createAdapter } from '@socket.io/redis-adapter';
// import { createClient } from "redis";


// export default class SocketAdapter {

//     private socketpub: any;
//     private socketsub: any;

//     constructor(connectionOptions: any) {
//         this.socketpub = createClient(connectionOptions);
//         this.socketsub = createClient(connectionOptions);
//     }

//     initAdapter = () => (
//         createAdapter(this.socketpub, this.socketsub)
//     );

// }









// // import ioRedis from "socket.io-redis";
// // import { createClient } from "redis";


// // export default class SocketAdapter {

// //     private socketpub: any;
// //     private socketsub: any;

// //     constructor(connectionOptions: ClientOpts) {
// //         this.socketpub = createClient(connectionOptions);
// //         this.socketsub = createClient(connectionOptions);
// //     }

// //     initAdapter = () => (
// //         ioRedis({ pubClient: this.socketpub, subClient: this.socketsub })
// //     );

// // }



